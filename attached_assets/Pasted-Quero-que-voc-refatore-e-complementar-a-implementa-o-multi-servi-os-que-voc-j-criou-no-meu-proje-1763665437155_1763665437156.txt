Quero que você refatore e complementar a implementação multi-serviços que você já criou no meu projeto VectorPro, seguindo estes pontos obrigatórios.

Contexto atual resumido:
Você já implementou:

tabela services

tabela user_services

tabela service_categories

tabelas removeBgPlans, removeBgUsage, removeBgApiKeys

ajustes em payments e credentials com serviceId

tela AdminServiceManagement com CRUD de serviços

endpoints:

GET/POST/PUT/DELETE /api/admin/services

GET /api/marketplace/services

POST /api/marketplace/subscribe

GET /api/user-services

dashboard do cliente com sidebar e uma página chamada Marketplace

Agora preciso que você corrija pontos críticos e evolua a lógica de forma segura, sem quebrar fluxo atual. Importante: eu quero substituir a ideia de “Marketplace” pelo conceito mais claro de “Serviços”.

Requisitos detalhados:

Trocar “Marketplace” por “Serviços” na UI e rotas

Renomear na interface do cliente:

Menu “Marketplace” deve virar “Serviços”

Se existir “MarketplacePage” visível para o usuário, renomear o título e textos para “Serviços” ou “Explorar serviços”

No painel admin:

A aba ou sessão chamada “Marketplace” deve ser renomeada visualmente para “Serviços”

Nas rotas de API:

Criar novos endpoints:

GET /api/services (equivalente a /api/marketplace/services)

POST /api/services/subscribe (equivalente a /api/marketplace/subscribe)

Manter os endpoints antigos /api/marketplace/* como alias temporário, redirecionando ou chamando internamente os novos, para não quebrar o frontend enquanto eu ajusto

Toda nomenclatura voltada ao usuário final deve falar em “Serviços” e “Meus serviços”, não “Marketplace”

Finalizar backend de planos e categorias
Atualmente, service-plans e categories estão como placeholder. Preciso que você:

Implemente no storage:

createServicePlan

updateServicePlan

deleteServicePlan

getServicePlansByServiceId

createServiceCategory

updateServiceCategory

deleteServiceCategory

getAllServiceCategories

Implemente endpoints REST reais para:

GET/POST/PUT/DELETE /api/admin/service-plans

GET/POST/PUT/DELETE /api/admin/service-categories

Conecte a tela AdminServiceManagement para salvar e carregar planos e categorias do banco, não mais mocks em memória

Usar Zod para validar payloads das rotas admin de serviços, planos e categorias, para evitar dados inválidos que possam quebrar pagamentos ou UI

Integrar corretamente user_services com pagamentos e acesso
Hoje já existe user_services, mas preciso garantir a lógica completa:

Quando o usuário assinar um serviço:

a rota /api/services/subscribe deve:

validar serviceId e planId

criar ou atualizar um registro em user_services com:

userId

serviceId

planId

status adequado (por exemplo, PENDENTE até pagamento)

dados do ciclo se já existir esse campo

Quando o PIX for pago e o webhook confirmar:

garantir que o payment tenha serviceId e, se possível, planId

localizar o user_services correspondente

atualizar user_services.status para ATIVO

atualizar campos de ciclo ou proximo pagamento, se existirem

No painel do usuário:

a lista de “Meus serviços” deve vir de user_services + services

apenas serviços com user_services.status = ATIVO devem exibir credenciais e botões de acesso

serviços sem user_services ou com status INATIVO devem mostrar botões de “Assinar” ou “Ativar assinatura”

Ajustar lógica de status do usuário e do serviço
As regras de negócio são:

INATIVO:

o usuário consegue logar normalmente

no dashboard, não tem nada desbloqueado

para cada serviço, ele só vê as opções de assinar/pagar

ATIVO:

o usuário consegue logar

vê credenciais e acesso para os serviços que têm user_services.status = ATIVO

BLOQUEADO:

o usuário não pode logar

ou, se preferir, pode até logar, mas deve ser bloqueado com tela de aviso para falar com o suporte
Quero que:

o controle de acesso a cada serviço seja baseado em user_services.status

o status global BLOQUEADO continue respeitado na lógica de login

o status INATIVO seja tratado como “sem serviços ativos, mas pode assinar”

Corrigir e organizar a lógica de pagamentos pendentes
Preciso de um comportamento mais seguro e previsível para pagamentos pendentes:

Quando o usuário gerar um novo PIX para o mesmo serviço/plano:

ou:

marcar automaticamente os pagamentos pendentes anteriores daquele userId + serviceId como “expired” ou “canceled_by_system”

ou:

bloquear a criação de novos pagamentos pendentes se já existir um pending recente, retornando mensagem clara para a UI

Quando o PIX for confirmado via webhook:

o pagamento que ficou “paid” deve:

ativar o user_services correspondente

limpar outros pagamentos pendentes antigos daquele userId + serviceId (marcá-los como “obsolete”, “expired” ou similar)

No histórico de pagamentos exibido para o usuário:

o foco deve ser nos pagamentos pagos e no último pending relevante

pagamentos pendentes obsoletos devem ser ignorados ou escondidos na UI padrão

Prepare RemoveBG para arquitetura genérica, sem quebrar o que existe
Atualmente você criou tabelas específicas:

removeBgPlans

removeBgUsage

removeBgApiKeys
Isso é aceitável por enquanto, mas quero que você:

documente claramente onde o código do RemoveBG está acoplado

deixe claro um ponto único de integração, por exemplo um módulo de “serviceAdapters” ou “serviceIntegrations”

onde for possível, use serviceId e uma abordagem que permita no futuro generalizar para serviceUsage e serviceApiKeys, sem precisar criar uma tabela nova para cada serviço
Não é necessário reescrever tudo agora, mas organize de forma que o RemoveBG seja o “primeiro cliente” de uma arquitetura mais genérica, não uma exceção espalhada pelo código.

Refino técnico e organização de código

AdminServiceManagement está muito grande. Comece uma refatoração mínima:

separar em componentes menores:

ServicesList

ServiceForm

PlansSection

CategoriesSection

Remover lógica duplicada

Centralizar tipos e schemas em arquivos bem definidos

Se possível, usar react-hook-form + Zod nos formulários mais críticos de admin

Regras gerais:

Não quebrar nenhum fluxo atual de login, pagamento ou acesso ao Vectorizer

Manter o serviço Vectorizer funcionando como está

Manter compatibilidade com serviceId = 1

Priorizar segurança, consistência de dados e clareza na UI

Em toda a UI voltada ao cliente, troque o conceito de “Marketplace” por “Serviços” e “Meus serviços”

No final, me entregue um resumo das mudanças feitas, arquivos alterados e de como ficou a lógica final de:

criação de serviços

assinatura

pagamento

ativação via webhook

exibição no painel do usuário